# Conversion-only patch series (apply order)
# Quilt-style ordered list. Each line: <patch-filename>
# Applied by build/build-wasm.sh AFTER wasm-build-fixes.patch when CONVERSION_ONLY=1.
#
#   exports -- LOK EXPORTED_FUNCTIONS link contract (Executable_soffice_bin.mk)
#   shims   -- non-conversion LOK shim bodies (desktop/source/lib/init.cxx)
#   ui-sd   -- gate sd/source/ui CUT-safe subdirs on DISABLE_GUI (Library_sd.mk only)
#
# ui-sc is TEMPORARILY excluded: sc/source/ui is a dense web — scdll/scmod/
# docshell (load-bearing, KEEP) reference most CUT dirs (link errors in
# 31014292563). Reintroduce after sd-only green confirms the lever + size.
wasm-trim-lok-exports-conversion-only.patch
wasm-trim-lok-shims-conversion-only.patch
wasm-trim-ui-sd-conversion-only.patch
